import { Meta, Primary, Stories, Controls } from "@storybook/blocks";

import * as HeaderStories from "./Header.stories.ts";

<Meta of={HeaderStories} name="Info" />

# Header

Header takes as props: path to icon, font color, background color and header text.

<Primary />

<Controls />

<Stories />
